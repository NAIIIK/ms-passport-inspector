spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://postgres:5432/passport_inspector}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:postgres}

minio:
  url: ${MINIO_URL:http://minio:9000}

smev:
  client:
    url: ${SMEV_CLIENT_URL:http://smev-api-mock:8081}
